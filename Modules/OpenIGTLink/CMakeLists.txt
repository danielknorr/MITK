find_package(OpenIGTLink)

include(${OpenIGTLink_USE_FILE})

MITK_CREATE_MODULE(
  PACKAGE_DEPENDS Qt4
  DEPENDS MitkCore
  ADDITIONAL_LIBS ${OpenIGTLink_LIBRARIES}
  EXPORT_DEFINE MITK_OPENIGTLINK_EXPORT
)
